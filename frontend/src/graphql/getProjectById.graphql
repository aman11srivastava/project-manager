query getProjectDetails ($id: ID!) {
    project(id: $id) {
        id
        name
        status
        description
        client {
            name
            email
            phone
            id
        }
    }
}
